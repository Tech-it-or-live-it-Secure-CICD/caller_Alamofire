name: "calling_workflow"
on:
  workflow_call:
  workflow_dispatch:

jobs:
  call-workflow-1-in-local-repo:
      uses: Tech-it-or-live-it-Secure-CICD/centralWorkflows/.github/workflows/centralTEST.yml@main
      secrets: inherit
  retrieveID:
   runs-on: ubuntu-latest
   steps:
   - run: |
        curl https://raw.githubusercontent.com/Tech-it-or-live-it-Secure-CICD/centralWorkflows/main/engagementIDs.json > test.json
        echo "ID= $(jq '."${{ github.repository }}".${{ github.ref_name }}.anchoreID' test.json)"

        





    #uses: ${{ github.repository_owner }}/centralWorkflows/.github/workflows/centralTEST.yml@main
